# ---------------------------------------------------------------------------
# LegalMove - Autonomous Contract Comparison Agent
# Pinned, mutually-compatible versions verified May 2026.
#
#   langchain-openai 1.2.1 requires:  openai>=2.26,<3  AND  langchain-core>=1.3.2,<2
#   Langfuse v4 (4.x) is the current SDK: start_as_current_observation +
#   propagate_attributes + set_trace_io + mask, used for the hierarchical
#   pipeline trace with session/user/environment context.
# ---------------------------------------------------------------------------
openai==2.37.0
langchain==1.3.1
langchain-openai==1.2.1
langchain-core==1.4.0
langfuse==4.6.1
pydantic==2.11.7
python-dotenv==1.0.1

# Front-end (Streamlit wizard + Streamlit Community Cloud deploy)
streamlit==1.57.0

# Dev-only (NOT required at runtime): regenerating the synthetic test
# contracts in data/test_contracts/.  Install manually if you need to
# re-render the images:  pip install pillow==11.0.0
